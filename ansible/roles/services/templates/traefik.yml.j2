api:
  dashboard: true

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  le:
    acme:
      email: "{{ traefik_acme_email }}"
      storage: /acme.json
      tlsChallenge: {}
